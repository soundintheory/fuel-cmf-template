<ul id="nav">
    {% for page in level1 %}
	<li class="{{ (loop.index == loop.length) ? 'last' : '' }}{{ page.active ? ' active' : '' }}">
	    <a href="{{ page.url }}">{{ page.name }}</a>
	</li>
	{% endfor %}
</ul>